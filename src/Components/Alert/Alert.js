import {Alert} from "react-bootstrap";
import React from "react";

const AlertBox = ()=>(
    <Alert dismissible variant="danger">
        <Alert.Heading>Oh snap! You got an error!</Alert.Heading>
        <p>
            User credentials do no m
        </p>
    </Alert>
);

export  default  AlertBox;